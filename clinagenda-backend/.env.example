# Copy this file to `.env` and fill in the blank values.
# `.env` is ignored by git and must never be committed.

# MySQL, created by docker compose on first start. The API reads DB_USER and
# DB_PASSWORD to build its connection string (see appsettings.json).
DB_USER=
DB_PASSWORD=
DB_ROOT_PASSWORD=
